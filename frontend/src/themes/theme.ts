import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2196f3',
    },
    secondary: {
      main: '#aa5722',
    },
    warning: {
      main: '#ff5722',
    },
    error: {
      main: '#ff5722',
    },
  },
  typography: {},
});

export default theme;
